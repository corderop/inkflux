---
import LoginForm from "../components/LoginForm/LoginForm.astro";

interface Props {
  hasError: boolean;
}

const { hasError } = Astro.props;
---

<LoginForm method="POST" action="/api/login" hasError={hasError} />
